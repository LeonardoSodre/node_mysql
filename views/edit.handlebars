<h2> Editar Livro </h2>


<form action="/edit/save" method="post">

    <input type="hidden" value="{{book.id}}" name="id">

    <div> 
        <label for="title">Titulo: </label>
        <input type="text" name="titulo" placeholder="Digite o titulo" id="title" value="{{book.title}}">
    </div>
    <div>
        <label for="">Numero de paginas</label>
        <input type="number" name="pageqty" placeholder="Quantas paginas o livro possui?" id="pageqty" value="{{book.pageqty}}">
    </div>
    <button type="submit"> Enviar</button>
</form>